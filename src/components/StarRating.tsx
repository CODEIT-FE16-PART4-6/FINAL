'use client';
import { useState } from 'react';

/*
const StarRatingProps {
    value?: number;
    
}

const Star ()=>{


}*/
export const StarRating = () => {
  const [currentRating, setCurrentRating] = useState(); // 현재 선택된 값.
  const [hoverRating, setHoverRating] = useState(); //마우스 를 올렸을 때의 임시값
  //[confirm, setConfirm] =useState(); //점수 확정 상태

  return <>!!! starRatingTest</>;
};
